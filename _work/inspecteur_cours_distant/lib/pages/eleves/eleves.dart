import 'package:flutter/material.dart';

class Eleves extends StatelessWidget {
  //
  List eleves = [];
  //
  @override
  Widget build(BuildContext context) {
    //
    return Scaffold(
      appBar: AppBar(),
      body: ListView(padding: EdgeInsets.all(15), children: []),
    );
  }

  //
}
